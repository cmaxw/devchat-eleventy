{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "eleventy build",
            "type": "shell",
            "command": "node --max_old_space_size=8192 node_modules/.bin/eleventy",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}